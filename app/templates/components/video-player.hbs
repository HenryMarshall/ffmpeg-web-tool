{{#hb-videojs 
    isPaused=isPaused
    controls=controls
    currentTime=currentTime
    duration=duration
    height=height
    loop=loop
    muted=muted
    playbackRate=playbackRate
    width=width
    volume=volume

    setEndpoint="setEndpoint"
    startEndpoint=startEndpoint
    endEndpoint=endEndpoint
}}

  {{hb-videojs-source src="/assets/test_video.webm"}}

{{/hb-videojs}}

<hr>

<div>
  <p>These start and end times are in the video-player component:</p>
  <p>{{startEndpoint}} | {{endEndpoint}}</p>
</div>